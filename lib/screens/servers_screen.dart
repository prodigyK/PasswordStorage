

import 'package:flutter/material.dart';

class ServersScreen extends StatelessWidget {
  static const routeName = '/servers-screen';

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Servers'),
      ),
    );
  }
}
